## 프로젝트 개요

**Local Document Organizer Agent**는 TXT, Markdown, PDF 문서를 업로드하면 문서 내용을 분석해 **문서 유형, 핵심 키워드, 요약, 추천 파일명, 중복 후보, Markdown 리포트**를 생성하는 AI 기반 웹 애플리케이션입니다.

이 프로젝트는 단순히 파일을 업로드하거나 저장하는 웹앱이 아닙니다. 업로드된 문서의 텍스트를 추출하고, AI 임베딩 모델을 사용해 문서의 의미를 벡터로 변환한 뒤, 문서 유형 분류와 유사도 비교를 수행합니다.

핵심 AI 기술은 **SentenceTransformer 기반 로컬 임베딩 모델**입니다.
사용한 임베딩 모델은 다음과 같습니다.

```text
sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
```

이 모델을 통해 문서 내용을 의미 벡터로 변환하고, 문서 유형 기준과의 유사도 비교 및 문서 간 중복 후보 탐지에 활용합니다.

---

## 왜 만들었는가

프로젝트와 과제를 진행하다 보면 README, 기획서, 학습자료, 포트폴리오 초안, TODO 문서처럼 다양한 파일이 계속 쌓입니다.

처음에는 파일명을 보고 구분할 수 있지만, 시간이 지나면 다음과 같은 문제가 생깁니다.

* `README.md` 파일이 여러 프로젝트에 반복해서 존재함
* `final.txt`, `document.pdf`, `정리본.md`처럼 파일명만으로 내용을 알기 어려움
* 어떤 문서가 최신인지 직접 열어보기 전까지 확인하기 어려움
* 비슷한 초안이나 중복 문서가 여러 개 저장됨
* 문서를 정리하려면 내용을 하나씩 읽어야 해서 시간이 오래 걸림

이 문제를 해결하기 위해, 사용자가 문서를 업로드하면 시스템이 문서 내용을 분석하고 정리 기준을 자동으로 제안하는 웹 애플리케이션을 만들었습니다.

즉, 이 프로젝트의 목적은 **파일을 저장하는 것**이 아니라, **문서 내용을 이해하고 정리 판단을 도와주는 AI 기반 문서 관리 보조 도구를 만드는 것**입니다.

---

## 입력

사용자는 Streamlit 웹 화면에서 문서 파일을 업로드합니다.

지원하는 입력 형식은 다음과 같습니다.

* TXT
* Markdown
* PDF

입력 문서 예시는 다음과 같습니다.

* 프로젝트 README
* 과제 기획서
* 학습 정리 노트
* 포트폴리오 초안
* 자기소개서 초안
* TODO 문서
* 기능 설명 문서

이 프로젝트는 파일명만 보는 것이 아니라, 업로드된 문서 내부의 텍스트를 추출해 분석합니다.

---

## 사용한 AI 기술

이 프로젝트에서 사용한 핵심 AI 기술은 **SentenceTransformer 임베딩 모델**입니다.

### 사용 모델

```text
sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
```

이 모델은 문장이나 문단을 숫자 벡터로 변환하는 임베딩 모델입니다.
단순히 단어가 일치하는지만 보는 것이 아니라, 문장의 의미가 얼마나 비슷한지 비교할 수 있도록 텍스트를 벡터 공간에 표현합니다.

예를 들어 다음 문장들은 표현은 다르지만 의미가 비슷합니다.

```text
프로젝트 실행 방법을 설명합니다.
설치 및 실행 절차를 안내합니다.
```

일반적인 키워드 규칙만 사용하면 두 문장을 다르게 볼 수 있지만, 임베딩 모델은 두 문장의 의미적 유사성을 비교할 수 있습니다.

따라서 이 프로젝트에서는 임베딩 모델을 사용해 다음 기능을 구현했습니다.

* 문서 내용을 벡터로 변환
* 문서 유형 기준과 업로드 문서 간 의미 유사도 비교
* 여러 문서 간 유사도 비교
* 중복 후보 탐지
* 규칙 기반 보정과 결합한 문서 유형 분류

---

## 왜 임베딩 모델이 AI인가

임베딩 모델은 사람이 직접 작성한 단순 조건문이나 키워드 목록이 아닙니다.

`sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`는 Transformer 구조를 기반으로 학습된 자연어 처리 모델이며, 문장의 의미를 벡터로 변환합니다. 이 벡터는 문서 간 의미적 유사도를 계산하는 데 사용됩니다.

즉, 이 프로젝트에서 AI는 다음 방식으로 사용됩니다.

```text
문서 텍스트
→ SentenceTransformer 임베딩 모델
→ 의미 벡터 생성
→ 문서 유형 기준과 유사도 비교
→ 문서 유형 분류 및 중복 후보 탐지
```

따라서 이 프로젝트의 AI 기능은 단순한 CRUD나 파일 관리가 아니라, **학습된 NLP 임베딩 모델을 이용해 문서 의미를 분석하는 과정**에 있습니다.

---

## AI 기능이 핵심인 이유

이 프로젝트의 핵심은 문서 내용을 분석해 정리 결과를 자동으로 생성하는 것입니다.

주요 AI 및 지능형 처리 기능은 다음과 같습니다.

### 1. 로컬 임베딩 기반 문서 유형 분류

업로드된 문서의 텍스트를 SentenceTransformer 모델로 임베딩한 뒤, 문서 유형별 기준과 유사도를 비교합니다.

이를 통해 문서를 다음과 같은 유형으로 분류합니다.

* 프로젝트 README
* 기획서
* 학습자료
* 포트폴리오 문서
* TODO 또는 작업 메모
* 일반 문서

단순히 확장자나 파일명만 보는 것이 아니라, 문서 내부 텍스트의 의미를 기준으로 분류합니다.

### 2. 규칙 기반 보정

임베딩 유사도만으로 부족한 경우를 보완하기 위해 규칙 기반 보정을 함께 사용합니다.

예를 들어 README 문서에는 다음과 같은 표현이 자주 등장합니다.

* 설치 방법
* 실행 방법
* 주요 기능
* 프로젝트 구조
* requirements
* 사용 방법

이러한 표현을 함께 참고해 문서 유형 분류의 안정성을 높입니다.

### 3. 핵심 키워드 추출

문서에서 자주 등장하거나 의미상 중요한 단어와 표현을 추출합니다.

이를 통해 사용자는 문서 전체를 읽지 않아도 문서의 주제를 빠르게 파악할 수 있습니다.

### 4. 문서 요약

배포 환경에서는 핵심 문장 기반 추출형 요약을 기본으로 사용합니다.

로컬 실행 환경에서는 Ollama가 설치되어 있고 모델이 준비된 경우, 로컬 LLM 기반 요약을 선택적으로 사용할 수 있습니다.

즉, 배포 데모의 기본 AI 핵심은 SentenceTransformer 임베딩 기반 분석이며, Ollama는 로컬 환경에서 사용할 수 있는 선택 기능입니다.

### 5. 추천 파일명 생성

문서 유형, 프로젝트명, 핵심 키워드를 바탕으로 사용자가 이해하기 쉬운 파일명을 추천합니다.

예를 들어 `document.pdf`, `final.txt`, `README.md`처럼 모호한 이름 대신 문서 내용을 반영한 파일명을 제안합니다.

### 6. 중복 후보 탐지

여러 문서를 업로드했을 때 문서 간 텍스트 유사도를 비교합니다.

이를 통해 오래된 초안, 비슷한 버전의 문서, 중복 저장된 파일을 중복 후보로 표시합니다.

---

## 구현 및 처리 과정

사용자가 문서를 업로드하면 시스템은 다음 순서로 동작합니다.

### 1. 문서 업로드

사용자가 Streamlit 웹 화면에서 TXT, Markdown, PDF 문서를 업로드합니다.

### 2. 텍스트 추출

업로드된 파일에서 분석 가능한 텍스트를 추출합니다.

* TXT: 파일 내용을 텍스트로 읽음
* Markdown: Markdown 문법이 포함된 텍스트를 읽음
* PDF: PyMuPDF를 사용해 페이지별 텍스트를 추출함

### 3. 텍스트 품질 검사

추출된 텍스트가 분석 가능한 상태인지 확인합니다.

PDF에서 텍스트가 깨졌거나, 추출된 내용이 너무 부족한 경우에는 무리하게 분석하지 않고 확인이 필요한 문서로 분리합니다.

### 4. 텍스트 전처리

추출한 텍스트에서 불필요한 공백, 줄바꿈, 코드 블록, Mermaid 구조도 등 분석에 방해될 수 있는 요소를 정리합니다.

### 5. 임베딩 생성

전처리된 문서 텍스트를 `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2` 모델에 입력해 문서 의미를 나타내는 벡터를 생성합니다.

이 단계가 이 프로젝트의 핵심 AI 처리 과정입니다.

### 6. 문서 유형 분류

생성된 문서 임베딩을 문서 유형 기준과 비교해 가장 유사한 유형을 찾습니다.

이후 README, 기획서, 학습자료, 포트폴리오, TODO 등 문서 유형별 규칙을 함께 적용해 분류 결과를 보정합니다.

### 7. 키워드 추출

문서 내용에서 도메인 표현과 주요 단어를 추출합니다.

이를 통해 문서의 핵심 주제를 짧게 보여줍니다.

### 8. 프로젝트명 감지

README 문서나 프로젝트 설명 문서의 경우, 문서 내부에서 프로젝트명을 감지해 제목과 추천 파일명 생성에 활용합니다.

### 9. 요약 생성

배포 환경에서는 핵심 문장 기반 추출형 요약을 사용합니다.

로컬 실행 환경에서 Ollama가 준비되어 있으면 로컬 LLM 요약을 선택적으로 사용할 수 있습니다. Ollama 호출에 실패해도 앱이 중단되지 않고 기본 추출형 요약으로 대체됩니다.

### 10. 추천 파일명 생성

문서 유형, 프로젝트명, 핵심 키워드를 조합해 추천 파일명을 생성합니다.

### 11. 중복 후보 탐지

여러 문서를 업로드한 경우 문서 간 유사도를 비교해 내용이 비슷한 문서를 중복 후보로 표시합니다.

### 12. 결과 출력

분석 결과를 Streamlit 화면에 카드 형태로 표시하고, Markdown 리포트로 정리합니다.

---

## 출력

시스템은 업로드된 문서에 대해 다음 결과를 제공합니다.

* 원본 파일명
* 문서 표시명
* 문서 유형
* 문서 제목
* 핵심 키워드
* 문서 요약
* 추천 파일명
* 중복 후보
* Markdown 분석 리포트

예상 출력 예시는 다음과 같습니다.

```text
문서명: README.md
문서 유형: 프로젝트 README
핵심 키워드: Streamlit, 문서 분석, 파일명 추천, 중복 탐지
요약: 이 문서는 문서 업로드 후 유형 분류, 요약, 파일명 추천 기능을 제공하는 프로젝트 설명 문서입니다.
추천 파일명: local_document_organizer_readme.md
중복 후보: project_overview.md와 내용 유사
```

---

## 기술 구성

| 구분        | 사용 기술                                                       |
| --------- | ----------------------------------------------------------- |
| 웹 UI      | Streamlit                                                   |
| 개발 언어     | Python                                                      |
| 배포        | Hugging Face Spaces                                         |
| 소개 페이지    | GitHub Pages 또는 Cloudflare Pages 기반 정적 웹페이지                 |
| 입력 파일     | TXT, Markdown, PDF                                          |
| PDF 처리    | PyMuPDF                                                     |
| AI 임베딩 모델 | sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2 |
| 문서 유형 분류  | 로컬 임베딩 유사도 + 규칙 기반 보정                                       |
| 키워드 추출    | 도메인 표현 + 토큰 빈도 기반 추출                                        |
| 요약        | 핵심 문장 기반 추출형 요약                                             |
| 선택 기능     | Ollama 기반 로컬 LLM 요약                                         |
| 중복 탐지     | 문서 간 텍스트/의미 유사도 비교                                          |
| 결과 출력     | Streamlit 화면 + Markdown 리포트                                 |

---

## AI와 웹이 함께 사용된 방식

이 프로젝트는 **AI 기능과 웹 기능이 함께 동작하는 구조**입니다.

웹 기능은 사용자가 브라우저에서 문서를 업로드하고 결과를 확인할 수 있도록 Streamlit으로 구현했습니다.

AI 기능은 업로드된 문서의 텍스트를 임베딩 모델로 분석해 문서 유형 분류와 유사도 비교에 활용합니다.

전체 흐름은 다음과 같습니다.

```text
사용자 웹 입력
→ TXT / Markdown / PDF 업로드
→ 텍스트 추출
→ SentenceTransformer 임베딩 모델로 문서 의미 벡터 생성
→ 문서 유형 분류
→ 키워드 추출
→ 요약 생성
→ 추천 파일명 생성
→ 중복 후보 탐지
→ 웹 화면 및 Markdown 리포트 출력
```

따라서 이 프로젝트는 단순한 정적 페이지가 아니라, **웹 화면에서 사용자 입력을 받고 AI 기반 문서 분석 결과를 출력하는 AI 웹 애플리케이션**입니다.

---

## 과제 조건 충족 여부

| 필수 조건            | 충족 내용                                                                    |
| ---------------- | ------------------------------------------------------------------------ |
| AI 기능이 포함되어야 함   | SentenceTransformer 임베딩 모델을 사용해 문서 의미 벡터를 생성하고, 문서 유형 분류와 유사도 비교에 활용합니다. |
| 웹 기능이 포함되어야 함    | Streamlit 웹앱으로 문서 업로드, 분석 실행, 결과 확인 기능을 제공합니다.                           |
| 사용자 입력이 있어야 함    | 사용자가 TXT, Markdown, PDF 문서를 업로드합니다.                                      |
| 처리 과정이 있어야 함     | 텍스트 추출, 품질 검사, 전처리, 임베딩 생성, 유형 분류, 키워드 추출, 요약, 중복 탐지를 수행합니다.             |
| 출력 결과가 있어야 함     | 문서 유형, 핵심 키워드, 요약, 추천 파일명, 중복 후보, Markdown 리포트를 출력합니다.                   |
| 단순 CRUD가 아니어야 함  | 파일 저장/조회가 아니라 문서 내용을 분석해 새로운 정리 결과를 생성합니다.                               |
| 외부 API 의존을 줄여야 함 | 기본 기능은 외부 API 없이 로컬 임베딩 모델과 추출형 요약으로 동작합니다.                              |
| 배포되어야 함          | Hugging Face Spaces에 Streamlit 앱으로 배포했습니다.                               |
| 로컬 LLM 확장 가능     | Ollama 기반 로컬 LLM 요약은 로컬 실행 환경에서 선택적으로 사용할 수 있습니다.                        |

---

## 배포 및 실행 링크

* 실행 데모: https://skyleaping-local-document-organizer-agent.hf.space
* Hugging Face Space: https://huggingface.co/spaces/skyleaping/local-document-organizer-agent
* 소개 페이지 저장소: https://github.com/lightleaping/s23.aiweb2026.site
* 소개 페이지: https://s23.aiweb2026.site

---

## 배포 환경에서의 동작 기준

Hugging Face Spaces 배포 환경에서는 사용자의 로컬 PC에서 실행되는 Ollama 서버에 직접 접근할 수 없습니다.

따라서 배포 데모에서는 로컬 Ollama 요약 기능을 기본 비활성화하고, 다음 기능을 중심으로 제공합니다.

* 문서 업로드
* 텍스트 추출
* 텍스트 품질 검사
* SentenceTransformer 임베딩 기반 문서 유형 분류
* 핵심 키워드 추출
* 추출형 요약
* 추천 파일명 생성
* 중복 후보 탐지
* Markdown 리포트 생성

로컬 PC에서 직접 실행하는 경우에는 Ollama를 실행한 뒤 로컬 LLM 요약 기능을 선택적으로 사용할 수 있습니다.

---

## 기대 효과

이 프로젝트를 사용하면 문서 정리에 드는 시간을 줄일 수 있습니다.

특히 다음과 같은 상황에서 도움이 됩니다.

* 프로젝트별 README와 문서가 여러 개 쌓였을 때
* 과제 자료와 학습 노트를 정리해야 할 때
* 포트폴리오나 자기소개서 초안이 여러 버전으로 나뉘었을 때
* 비슷한 내용의 문서를 찾고 정리해야 할 때
* 문서 내용을 빠르게 파악하고 적절한 파일명으로 저장하고 싶을 때

결과적으로 사용자는 문서를 하나씩 열어 확인하는 시간을 줄이고, AI가 제안한 기준을 바탕으로 더 체계적으로 파일을 관리할 수 있습니다.

---

## 한계와 개선 방향

현재 프로젝트는 문서 텍스트를 기반으로 분석하기 때문에 PDF에서 텍스트가 제대로 추출되지 않는 경우 분석 품질이 떨어질 수 있습니다.

또한 문서 유형 분류와 추천 파일명은 문서 내용에 따라 달라질 수 있으므로, 완전히 확정된 결과라기보다는 사용자가 문서를 정리할 때 참고할 수 있는 보조 결과로 보는 것이 적절합니다.

배포 환경에서는 사용자의 로컬 Ollama 서버를 직접 사용할 수 없기 때문에, 로컬 LLM 요약 기능은 배포 데모에서 제한됩니다. 하지만 핵심 AI 기능인 SentenceTransformer 임베딩 기반 문서 분석은 배포 환경에서도 사용할 수 있도록 구성했습니다.

향후에는 다음과 같은 방향으로 개선할 수 있습니다.

* OCR 기능을 추가해 이미지 기반 PDF도 분석 가능하게 만들기
* 사용자가 직접 문서 유형 기준을 추가할 수 있도록 개선하기
* 프로젝트별 폴더 추천 기능 추가하기
* 분석 결과를 CSV 또는 JSON으로 저장하기
* 여러 문서를 한 번에 비교하는 대시보드 기능 추가하기
* 문서 버전 관리 기능 추가하기
* 배포 환경에서도 안정적으로 사용할 수 있는 LLM 요약 방식 추가하기

---

## 마무리

Local Document Organizer Agent는 단순한 파일 업로드 웹앱이 아니라, 사용자의 문서 정리 과정을 돕는 **AI 기반 문서 관리 보조 도구**입니다.

이 프로젝트는 웹 화면에서 문서를 입력받고, AI 임베딩 모델로 문서의 의미를 분석한 뒤, 문서 유형·키워드·요약·추천 파일명·중복 후보를 생성합니다.

이를 통해 AI를 단순한 답변 생성 도구가 아니라, 실제 개인 작업 흐름을 개선하는 자동화 도구로 활용할 수 있음을 보여주고자 했습니다.
