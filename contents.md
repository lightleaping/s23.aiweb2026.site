## 프로젝트 개요

**Local Document Organizer Agent**는 사용자가 업로드한 문서를 분석해 **문서 유형, 핵심 키워드, 요약, 추천 파일명, 중복 후보**를 제안하는 AI 기반 문서 정리 웹 애플리케이션입니다.

프로젝트를 진행하다 보면 README, 기획서, 학습자료, 포트폴리오 초안, TODO 문서처럼 다양한 파일이 계속 쌓입니다. 시간이 지나면 파일명만 보고 어떤 문서가 최신인지, 어떤 파일이 어떤 프로젝트와 관련 있는지 판단하기 어려워집니다.

이 프로젝트는 문서를 하나씩 열어 확인하지 않아도, 문서 내용을 기반으로 정리 기준을 자동으로 제안하는 것을 목표로 만들었습니다.

---

## 실행 데모 및 저장소

- 실행 데모: https://skyleaping-local-document-organizer-agent.hf.space
- 소개 페이지 저장소: https://github.com/lightleaping/s23.aiweb2026.site
- 소개 페이지: https://s23.aiweb2026.site

현재 데모는 Hugging Face Spaces에 배포되어 있으며, TXT, Markdown, PDF 문서를 업로드해 분석 결과를 확인할 수 있습니다.

파일 업로드 기능이 포함되어 있어 Streamlit 배포 환경에서는 `.streamlit/config.toml` 설정을 함께 사용합니다.

---

## 왜 만들었는가

개발 프로젝트와 과제를 진행하면서 여러 문서가 반복적으로 생성되었습니다.

예를 들어 프로젝트마다 README가 있고, 수업 자료와 과제 정리 파일, 자기소개서나 포트폴리오 초안, 기능 설명 문서가 따로 존재합니다. 하지만 시간이 지나면 `final.txt`, `document.pdf`, `README.md`, `정리본.md`처럼 모호한 이름의 파일이 많아지고, 어떤 문서가 어떤 내용을 담고 있는지 확인하기 어려워집니다.

그래서 문서를 업로드하면 시스템이 자동으로 내용을 분석하고, 문서 유형과 핵심 키워드, 요약, 추천 파일명을 제안하는 웹 애플리케이션을 만들고자 했습니다.

---

## AI 기능이 핵심인 이유

이 프로젝트의 핵심은 단순한 파일 업로드나 저장 기능이 아니라, **문서 내용을 분석해 정리 기준을 자동으로 제안하는 AI 기반 처리 흐름**입니다.

일반적인 파일 관리 도구는 파일명, 확장자, 생성일, 수정일을 기준으로 문서를 보여줍니다. 반면 이 프로젝트는 문서 내부 텍스트를 분석해 다음 결과를 생성합니다.

- 문서 내용 기반 문서 유형 분류
- 핵심 키워드 추출
- 핵심 문장 기반 문서 요약
- 문서 내용에 맞는 추천 파일명 생성
- 여러 문서 간 유사도 비교를 통한 중복 후보 탐지

배포 환경에서는 안정성을 위해 핵심 문장 기반 추출형 요약을 기본으로 사용하고, 로컬 실행 환경에서는 Ollama 기반 로컬 LLM 요약을 선택적으로 사용할 수 있도록 구성했습니다.

---

## 입력 데이터

사용자는 웹 화면에서 문서 파일을 업로드합니다.

지원하는 입력 형식은 다음과 같습니다.

- TXT 문서
- Markdown 문서
- PDF 문서

입력 문서는 파일명만 사용하는 것이 아니라, 문서 내부 텍스트를 추출한 뒤 분석 대상으로 사용합니다.

---

## 처리 흐름

사용자가 문서를 업로드하면 시스템은 다음 순서로 문서를 처리합니다.

1. **문서 업로드**
   - 사용자가 TXT, Markdown, PDF 파일을 업로드합니다.

2. **텍스트 추출**
   - TXT와 Markdown은 텍스트를 직접 읽습니다.
   - PDF는 PyMuPDF를 사용해 문서 내부 텍스트를 추출합니다.

3. **문서 전처리**
   - 추출된 텍스트에서 불필요한 공백과 줄바꿈을 정리합니다.
   - 분석 가능한 형태로 문서 내용을 변환합니다.

4. **문서 유형 분류**
   - 문서에 포함된 단어, 문장 구조, 주요 키워드를 바탕으로 문서 유형을 추정합니다.
   - README, 기획서, 학습자료, 포트폴리오 문서, TODO 문서, 일반 문서 등으로 분류합니다.

5. **핵심 키워드 추출**
   - 문서에서 반복되거나 중요도가 높은 단어를 추출합니다.
   - 이를 통해 문서의 주제를 빠르게 파악할 수 있게 합니다.

6. **문서 요약**
   - 배포 환경에서는 핵심 문장 기반 추출형 요약을 사용합니다.
   - 로컬 실행 환경에서는 사용자가 Ollama를 실행한 경우 로컬 LLM 요약을 선택적으로 사용할 수 있습니다.

7. **추천 파일명 생성**
   - 문서 유형과 핵심 키워드를 바탕으로 더 명확한 파일명을 제안합니다.
   - 예를 들어 `document.pdf`처럼 모호한 이름 대신 문서 주제가 반영된 이름을 추천합니다.

8. **중복 후보 탐지**
   - 여러 문서를 업로드한 경우 문서 간 유사도를 비교합니다.
   - 내용이 비슷한 문서를 중복 후보로 표시합니다.

9. **결과 출력**
   - 분석 결과를 Streamlit 화면에 표시합니다.
   - 필요할 경우 Markdown 리포트 형식으로 정리합니다.

---

## 출력 결과

시스템은 업로드된 문서에 대해 다음 결과를 제공합니다.

- 문서명
- 문서 유형
- 핵심 키워드
- 문서 요약
- 추천 파일명
- 중복 후보
- Markdown 분석 리포트

예상 출력 예시는 다음과 같습니다.

```text
문서명: README.md
문서 유형: 프로젝트 README
핵심 키워드: Streamlit, 문서 분석, 파일명 추천, 중복 탐지
요약: 이 문서는 문서 업로드 후 유형 분류와 요약, 파일명 추천 기능을 제공하는 프로젝트 설명 문서입니다.
추천 파일명: local_document_organizer_readme.md
중복 후보: project_overview.md와 내용 유사
```

---

## 주요 기능

### 1. 문서 업로드 및 텍스트 추출

사용자는 TXT, Markdown, PDF 문서를 업로드할 수 있습니다. 업로드된 문서는 내부적으로 텍스트를 추출한 뒤 분석 가능한 형태로 변환됩니다.

### 2. 문서 유형 분류

업로드된 문서의 내용을 바탕으로 문서 유형을 분류합니다. 단순히 파일 확장자만 보는 것이 아니라 문서 안에 포함된 문장, 키워드, 구조를 함께 참고합니다.

### 3. 핵심 키워드 추출

문서에서 중요한 단어나 표현을 추출합니다. 사용자는 문서 전체를 읽기 전에 해당 문서가 어떤 주제를 다루는지 빠르게 파악할 수 있습니다.

### 4. 문서 요약

업로드된 문서의 주요 내용을 짧게 요약합니다. 배포 환경에서는 핵심 문장 기반 추출형 요약을 기본으로 사용합니다. 로컬 실행 환경에서는 Ollama가 동작하는 경우 로컬 LLM 기반 요약을 선택적으로 사용할 수 있습니다.

### 5. 추천 파일명 생성

문서의 내용과 유형을 바탕으로 적절한 파일명을 추천합니다. 예를 들어 `README.md`, `document.pdf`, `final.txt`처럼 모호한 이름 대신 문서의 주제와 성격을 반영한 파일명을 제안합니다.

### 6. 중복 후보 탐지

여러 문서를 업로드했을 때 내용이 비슷한 문서가 있는지 확인합니다. 오래된 초안, 비슷한 버전의 문서, 중복 저장된 파일을 정리하는 데 도움을 줄 수 있습니다.

### 7. Markdown 리포트 생성

분석 결과는 화면에서 확인할 수 있을 뿐만 아니라 Markdown 형식의 리포트로도 정리할 수 있습니다. 리포트에는 문서 유형, 요약, 핵심 키워드, 추천 파일명, 중복 후보 등의 정보가 포함됩니다.

---

## 기술 구성

- **UI**: Streamlit
- **언어**: Python
- **문서 처리**: TXT, Markdown, PDF 텍스트 추출
- **PDF 처리**: PyMuPDF
- **문서 분석**: 문서 텍스트 기반 분류, 키워드 추출, 유사도 비교
- **요약 방식**: 핵심 문장 기반 추출형 요약
- **선택 기능**: Ollama 기반 로컬 LLM 요약
- **중복 탐지 방식**: 문서 간 텍스트 유사도 비교
- **결과 출력**: Streamlit 화면 + Markdown 리포트
- **배포 환경**: Hugging Face Spaces
- **소개 페이지**: GitHub Pages 또는 Cloudflare Pages 기반 정적 웹페이지

---

## 구현 조건 충족 여부

| 조건 | 충족 방식 |
|---|---|
| AI 기능이 핵심에 있음 | 문서 내용을 분석해 유형 분류, 키워드 추출, 요약, 추천 파일명, 중복 후보를 생성합니다. |
| 단순 CRUD가 아님 | 파일을 저장하거나 조회하는 앱이 아니라, 문서 내용을 분석해 정리 결과를 생성하는 앱입니다. |
| 사용자가 입력을 제공함 | 사용자는 TXT, Markdown, PDF 문서를 업로드합니다. |
| 입력을 처리함 | 업로드된 문서에서 텍스트를 추출하고 전처리한 뒤 분석합니다. |
| AI/지능형 결과를 출력함 | 문서 유형, 핵심 키워드, 요약, 추천 파일명, 중복 후보를 출력합니다. |
| 웹으로 실행 가능함 | Streamlit 기반 웹앱으로 실행되며 Hugging Face Spaces에 배포했습니다. |
| 외부 API 의존 최소화 | 배포 기본 기능은 외부 API 없이 동작하도록 구성하고, Ollama 기반 LLM 요약은 로컬 실행 시 선택 기능으로 분리했습니다. |
| 결과 확인 가능 | 웹 화면과 Markdown 리포트로 분석 결과를 확인할 수 있습니다. |

---

## 배포 환경에서의 동작 기준

로컬 LLM 요약 기능은 사용자의 PC 또는 실행 환경에서 Ollama가 동작하는 경우에 사용하는 선택 기능입니다. Hugging Face Spaces와 같은 배포 환경에서는 사용자의 로컬 PC에서 실행되는 Ollama 서버에 직접 접근할 수 없기 때문에 해당 기능은 제한될 수 있습니다.

따라서 배포 데모에서는 다음 기능을 중심으로 확인할 수 있습니다.

- 문서 업로드
- 텍스트 추출
- 문서 유형 분류
- 핵심 키워드 추출
- 추출형 요약
- 추천 파일명 생성
- 중복 후보 탐지
- Markdown 리포트 생성

---

## 기대 효과

이 프로젝트를 사용하면 문서 정리에 드는 시간을 줄일 수 있습니다.

특히 다음과 같은 상황에서 도움이 됩니다.

- 프로젝트별 README와 문서가 여러 개 쌓였을 때
- 과제 자료와 학습 노트를 정리해야 할 때
- 포트폴리오나 자기소개서 초안이 여러 버전으로 나뉘었을 때
- 비슷한 내용의 문서를 찾고 정리해야 할 때
- 문서 내용을 빠르게 파악하고 적절한 파일명으로 저장하고 싶을 때

결과적으로 사용자는 문서를 하나씩 열어 확인하는 시간을 줄이고, 시스템이 제안한 기준을 바탕으로 더 체계적으로 파일을 관리할 수 있습니다.

---

## 한계와 개선 방향

현재 프로젝트는 문서 텍스트를 기반으로 분석하기 때문에 PDF에서 텍스트가 제대로 추출되지 않는 경우 분석 품질이 떨어질 수 있습니다.

또한 문서 유형 분류와 추천 파일명은 문서 내용에 따라 달라질 수 있으므로, 완전히 확정된 결과라기보다는 사용자가 문서를 정리할 때 참고할 수 있는 보조 결과로 보는 것이 적절합니다.

배포 환경에서는 로컬 Ollama 서버를 직접 사용할 수 없기 때문에, 로컬 LLM 요약 기능은 제한될 수 있습니다. 이 부분은 향후 배포 환경에서도 안정적으로 동작하는 요약 방식으로 개선할 수 있습니다.

향후에는 다음과 같은 방향으로 개선할 수 있습니다.

- OCR 기능을 추가해 이미지 기반 PDF도 분석 가능하게 만들기
- 사용자가 직접 문서 유형 기준을 추가할 수 있도록 개선하기
- 프로젝트별 폴더 추천 기능 추가하기
- 분석 결과를 CSV 또는 JSON으로 저장하기
- 여러 문서를 한 번에 비교하는 대시보드 기능 추가하기
- 문서 버전 관리 기능 추가하기
- 배포 환경에서도 안정적으로 사용할 수 있는 요약 방식 추가하기

---

## 마무리

Local Document Organizer Agent는 단순한 파일 업로드 웹앱이 아니라, 사용자의 문서 정리 과정을 돕는 AI 기반 문서 관리 보조 도구입니다.

문서를 읽고, 분류하고, 요약하고, 이름을 추천하고, 중복 가능성까지 확인하는 흐름을 하나의 웹 애플리케이션으로 연결했습니다.

이 프로젝트를 통해 AI를 단순한 답변 생성 도구가 아니라, 실제 개인 작업 흐름을 개선하는 자동화 도구로 활용할 수 있음을 보여주고자 했습니다.
